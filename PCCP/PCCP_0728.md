# 문제 1
# 문제 설명

물류 시스템에서는 각 작업을 **5자리 정수**로 관리합니다.

각 작업 번호는 다음과 같은 형식입니다.

* **첫 번째 자리**: 작업 종류 (1~9)
* **나머지 네 자리**: 작업 번호

예를 들어,

* `51234` → 작업 종류 `5`, 작업 번호 `1234`
* `30001` → 작업 종류 `3`, 작업 번호 `0001`

작업은 다음 규칙에 따라 사원에게 배정됩니다.

* 작업 번호를 사원 수 `e`로 나눈 **나머지**를 담당 사원의 번호로 합니다.
* 단, 나머지가 `0`인 경우에는 **e번 사원**이 담당합니다.

예를 들어 사원 수가 5명이라면

| 작업 번호 | 담당 사원 |
| ----- | ----- |
| 1001  | 1     |
| 1002  | 2     |
| 1005  | 5     |
| 1010  | 5     |
| 1013  | 3     |

작업 목록 `works`, 사원 수 `e`, 작업 종류 `t`가 주어질 때, **작업 종류가 `t`인 작업을 가장 많이 처리한 사원의 번호를 반환**하세요.

처리한 작업 수가 가장 많은 사원이 여러 명이라면 **번호가 가장 작은 사원**을 반환합니다.

---

## 제한사항

* `1 ≤ works의 길이 ≤ 200,000`
* 각 작업은 `10000` 이상 `99999` 이하의 정수입니다.
* `2 ≤ e ≤ 100,000`
* `1 ≤ t ≤ 9`

---

## 입출력 예

| works                                 |  e |  t | result |
| ------------------------------------- | -: | -: | -----: |
| `[51234, 52345, 51239, 42344, 51244]` |  5 |  5 |      4 |
| `[10001,10002,10003,10004,10005]`     |  5 |  1 |      1 |
| `[21234,21239,21244,21249]`           |  5 |  2 |      4 |

---

## 입출력 예 설명

### 예제 1

작업 종류가 5인 작업은

* 1234 → 4번 사원
* 2345 → 5번 사원
* 1239 → 4번 사원
* 1244 → 4번 사원

따라서 4번 사원이 3개의 작업을 처리하여 정답은 `4`입니다.

---

### 예제 2

각 사원이 1개의 작업씩 처리하므로 가장 작은 번호인 `1`을 반환합니다.

---

### 예제 3

작업 번호의 담당 사원은

* 1234 → 4번
* 1239 → 4번
* 1244 → 4번
* 1249 → 4번

따라서 정답은 `4`입니다.

---

# 문제 2
조건을 반영하면 문제 정의를 조금 더 명확하게 수정할 수 있습니다.

---

# 문제 설명

문자열 비교 시스템을 개발하려고 합니다.

정답 문자열 `s`와 후보 문자열 배열 `arr`이 주어집니다.

각 후보 문자열은 아래 조건을 만족하면 **정답**으로 판정합니다.

## 정답 판정 조건

1. **문자열 비교는 대소문자를 무시합니다.**

예)

```
Hello World
hello world
HELLO WORLD
```

는 모두 같은 문자열로 판단합니다.

---

2. **띄어쓰기(공백)는 일부 생략될 수 있습니다.**

후보 문자열에서 정답 문자열에 존재하는 공백이 제거된 경우는 정답입니다.

예)

정답:

```
Hello World
```

후보:

```
HelloWorld
```

→ 정답

---

3. **띄어쓰기가 다른 위치에 존재하면 오답입니다.**

공백은 생략만 가능하며, 이동할 수 없습니다.

예)

정답:

```
Hello World
```

후보:

```
Hello Wor ld
```

→ 오답

---

4. **공백 관련 제한**

* 문자열의 맨 앞과 맨 뒤에는 공백이 존재하지 않습니다.
* 공백은 연속해서 2개 이상 등장하지 않습니다.

즉, 아래와 같은 문자열은 입력으로 주어지지 않습니다.

오답 예시:

```
" Hello"
"Hello "
"Hello  World"
```

---

정답 문자열 `s`와 후보 문자열 리스트 `arr`이 주어질 때, 각 후보 문자열이 정답인지 판단하여 결과 배열을 반환하세요.

* 정답이면 `1`
* 오답이면 `0`

---

## 제한사항

* `1 ≤ len(s) ≤ 100,000`
* `1 ≤ len(arr) ≤ 100,000`
* `arr[i]`는 영문자와 공백으로만 구성됩니다.
* 모든 문자열 길이의 합은 `1,000,000` 이하입니다.
* 입력 문자열은 위 공백 제한을 만족합니다.

---

## 입출력 예

### 예제 1

입력

```python
s = "Hello World"

arr = [
    "Hello World",
    "hello world",
    "HelloWorld",
    "Hello Wor ld",
    "HelloWorldd"
]
```

출력

```python
[1, 1, 1, 0, 0]
```

---

### 예제 2

입력

```python
s = "Artificial Intelligence"

arr = [
    "artificial intelligence",
    "ArtificialIntelligence",
    "Artificial Inte lligence"
]
```

출력

```python
[1, 1, 0]
```
---

# 문제 3
아래처럼 코딩테스트 형식으로 정리할 수 있습니다.

---

# 문제 설명

여러 개의 이진 문자열이 주어집니다.

각 문자열은 다음 방식으로 암호화되어 있습니다.

문자열에서 **연속된 `1`의 개수**를 순서대로 나열한 것이 해당 문자열의 암호입니다.

단, `0`은 연속된 `1` 구간을 구분하는 역할만 하며 암호에는 포함되지 않습니다.

예)

```
011010111
```

문자열을 구간별로 나누면

```
0 11 0 1 0 111
```

연속된 `1`의 개수는

```
[2, 1, 3]
```

입니다.

---

암호화 결과가 같은 문자열들은 **유사 문자열**로 판단합니다.

주어진 문자열 리스트에서 같은 암호를 가지는 문자열끼리 그룹화하여 반환하세요.

---

## 반환 조건

* 각 그룹은 원본 문자열의 **인덱스**를 공백으로 구분한 문자열로 반환합니다.
* 인덱스는 **1부터 시작**합니다.
* 각 그룹 내부의 인덱스는 오름차순이어야 합니다.
* 반환되는 그룹 리스트는 각 그룹의 **첫 번째 인덱스 기준 오름차순**이어야 합니다.

---

## 제한사항

* `1 ≤ len(arr) ≤ 100,000`
* `1 ≤ len(arr[i]) ≤ 100,000`
* 모든 문자열 길이의 합은 `1,000,000` 이하입니다.
* 문자열은 `0`, `1`로만 이루어져 있습니다.

---

## 입출력 예

### 예제 1

입력

```python
arr = [
    "011010111",
    "110111",
    "101",
    "1110",
    "011101"
]
```

각 문자열 암호:

| 인덱스 | 문자열       | 암호      |
| --- | --------- | ------- |
| 1   | 011010111 | [2,1,3] |
| 2   | 110111    | [2,3]   |
| 3   | 101       | [1,1]   |
| 4   | 1110      | [3]     |
| 5   | 011101    | [3,1]   |

출력

```python
[
 "1",
 "2",
 "3",
 "4",
 "5"
]
```

---

### 예제 2

입력

```python
arr = [
    "011",
    "110",
    "1011",
    "1110",
    "01011",
    "1101"
]
```

암호:

| 인덱스 | 문자열   | 암호    |
| --- | ----- | ----- |
| 1   | 011   | [2]   |
| 2   | 110   | [2]   |
| 3   | 1011  | [1,2] |
| 4   | 1110  | [3]   |
| 5   | 01011 | [1,2] |
| 6   | 1101  | [2,1] |

출력

```python
[
 "1 2",
 "3 5",
 "4",
 "6"
]
```

---

## 예제 3 (경계 테스트)

입력

```python
arr = [
    "1",
    "0",
    "000",
    "111111",
    "101010"
]
```

암호:

| 인덱스 | 암호      |
| --- | ------- |
| 1   | [1]     |
| 2   | []      |
| 3   | []      |
| 4   | [6]     |
| 5   | [1,1,1] |

출력

```python
[
 "1",
 "2 3",
 "4",
 "5"
]
```

---

# 문제 4

## 문제 설명

영문 대소문자와 `'['`, `']'`로만 이루어진 문자열 `message`가 주어집니다.

문자열에서 대괄호(`[]`)로 둘러싸인 부분은 **해당 문자열을 두 번 반복한 것**으로 해석합니다.

예를 들어,

* `"Ba[na]"` → `"Banana"`
* `"[Ba[na]]"` → `"BananaBanana"`

와 같이 해석됩니다.

대괄호는 항상 올바르게 짝이 맞으며, 중첩될 수 있습니다.

완전히 해석된 문자열에서 **1번부터 시작하는 인덱스** 기준으로 `l`번째 문자부터 `r`번째 문자까지를 반환하세요.

---

## 제한사항

* `1 ≤ len(message) ≤ 100,000`
* `message`는 영문 대소문자와 `'['`, `']'`로만 이루어져 있습니다.
* `'['`와 `']'`는 항상 올바르게 짝지어져 있습니다.
* 대괄호는 여러 단계로 중첩될 수 있습니다.
* 완전히 해석한 문자열의 길이는 `10^15` 이하입니다.
* `1 ≤ l ≤ r ≤` 해석된 문자열의 길이

---

## 입출력 예

| message          |  l |  r | result           |
| ---------------- | -: | -: | ---------------- |
| `"ABCDE"`        |  2 |  4 | `"BCD"`          |
| `"Ba[na]"`       |  1 |  6 | `"Banana"`       |
| `"Ba[na]A[p]le"` |  1 | 11 | `"BananaApple"`  |
| `"[Ba[na]]"`     |  1 | 12 | `"BananaBanana"` |

---

## 추가 테스트케이스

| message        |  l |  r | result             |
| -------------- | -: | -: | ------------------ |
| `"A[B]"`       |  1 |  3 | `"ABB"`            |
| `"A[B]"`       |  2 |  3 | `"BB"`             |
| `"[A]"`        |  1 |  2 | `"AA"`             |
| `"A[BC]D"`     |  1 |  6 | `"ABCBCD"`         |
| `"A[BC]D"`     |  3 |  5 | `"CBC"`            |
| `"[AB[C]]"`    |  1 |  8 | `"ABCCABCC"`       |
| `"[AB[C]]"`    |  4 |  8 | `"CABCC"`          |
| `"X[Y[Z]]"`    |  1 |  7 | `"XYZZYZZ"`        |
| `"A[B[C[D]]]"` |  1 | 15 | `"ABCDDCDDABCDDC"` |
| `"ABC"`        |  1 |  3 | `"ABC"`            |
